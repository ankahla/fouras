{% extends 'UserBundle::Profile/Vendor/layout.html.twig' %}

{% block body %}
<section id="manage-item-listing" class="manage-item-listing-main-block">
    <div class="container">
      {{ render(controller('UserBundle:Menu:index')) }}
      <p>
      <a class="btn btn-pink" href="{{ path('vendor_service_new') }}">{% trans %}Add new{% endtrans %}</a>
      </p>
      <div class="manage-item-listing-block">
        <table>
          <tbody><tr class="top-heading-row">
            <th class="vendor-img">Image</th>
            <th class="title">Title</th>
            <th class="address">Address</th>
            <th class="price">Price</th>
            <th class="action-icons">Action</th>
          </tr>
          {% for vendorService in vendor.services %}
          <tr>
            <td class="vendor-img"><img src="{{ asset(vendor_service_pic_dir ~ vendorService.picture) }}" width="200" class="img-responsive" alt="vendor-img"></td>
            <th class="title">{{ vendorService.service.name }}</th>
            <td class="address">{{ vendorService.address }}</td>
            <td class="price">{{ vendorService.costMin }}</td>
            <td class="action-icons">
              <div class="table-editable-icon">
                <a href="{{ path('vendor_service_edit', {id:vendorService.id}) }}"><i class="fa fa-edit" aria-hidden="true"></i></a>
                <a href="{{ path('vendor_service_delete', {id:vendorService.id}) }}"><i class="fa fa-trash-o" aria-hidden="true"></i></a>
              </div>
            </td>
          </tr>
          {% endfor %}
        </tbody></table>
      </div>
    </div>
  </section>
  {% endblock %}