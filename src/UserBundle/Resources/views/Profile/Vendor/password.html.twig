{% extends "FOSUserBundle::layout.html.twig" %}

{% block banner %}
{% if title is not defined %}
{% set title = 'Vendor dashboard' %}
{% endif %}
{% include 'front/banner.html.twig' with {'title' : title} %}
{% endblock %}

{% block body %}
<section id="couple_profile" class="couple-profile-main-page">
  <div class="container">
    {{ render(controller('UserBundle:Menu:index', {'_route': app.request.attributes.get('_route')})) }}
    <h5 class="description-heading">{% trans %}Change Password{% endtrans %}</h5>
    <div class="col-md-6">
    {% include 'UserBundle::Profile/password.form.html.twig' %}
    </div>
  </div>
</section>
{% endblock %}
