<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="{{ path('admin_dashboard') }}">FOURAS</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="{{ current_route == 'admin_translation' ? 'active' : '' }}"><a href="{{ path('admin_translation') }}">Gestion des traductions</a></li>

        <li class="dropdown">
          <a href="{{ path('admin_service_list') }}" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Gestion des services <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li class="{{ current_route == 'admin_service_list' ? 'active' : '' }}"><a href="{{ path('admin_service_list') }}">Liste des services</a></li>
            <li class="{{ current_route == 'admin_service_new' ? 'active' : '' }}"><a href="{{ path('admin_service_new') }}">Ajouter nouveau</a></li>
          </ul>
        </li>
        <li class="{{ current_route == 'admin_newsletter_subscribtion_list' ? 'active' : '' }}"><a href="{{ path('admin_newsletter_subscribtion_list') }}">Inscriptions newsletter</a></li>
        <li class="dropdown">
          <a href="{{ path('subscription_plan_index') }}" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Gestion des abonnements <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li class="{{ current_route == 'subscription_index' ? 'active' : '' }}"><a href="{{ path('subscription_index') }}">Liste des abonnées</a></li>
            <li class="{{ current_route == 'subscription_request_index' ? 'active' : '' }}"><a href="{{ path('subscription_request_index') }}">Liste des demandes</a></li>
            <li class="{{ current_route == 'subscription_plan_index' ? 'active' : '' }}"><a href="{{ path('subscription_plan_index') }}">Liste des packs</a></li>
            <li class="{{ current_route == 'subscription_feature_index' ? 'active' : '' }}"><a href="{{ path('subscription_feature_index') }}">Avantages abonnement</a></li>
          </ul>
        </li>
        <!--<li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Dropdown <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><a href="#">Action</a></li>
            <li><a href="#">Another action</a></li>
            <li><a href="#">Something else here</a></li>
            <li role="separator" class="divider"></li>
            <li><a href="#">Separated link</a></li>
            <li role="separator" class="divider"></li>
            <li><a href="#">One more separated link</a></li>
          </ul>
        </li>-->
      </ul>
      <!--
      <form class="navbar-form navbar-left">
        <div class="form-group">
          <input type="text" class="form-control" placeholder="Search">
        </div>
        <button type="submit" class="btn btn-default">Submit</button>
      </form>
      --->
      {% set locales = ['ar', 'fr', 'en'] %}
      <ul class="nav navbar-nav navbar-right">
        <li><a href="{{ path('homepage') }}" target="_blank">Voir site  <i class="fa fa-external-link" aria-hidden="true"></i></a></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">{{ current_locale }} <span class="caret"></span></a>
          <ul class="dropdown-menu">
            {% for locale in locales %}
              {% if current_locale != locale %}
            <li><a href="{{ path(current_route, app.request.attributes|merge({'_locale':locale})) }}" class="text-uppercase">{{ locale }}</a></li>
              {% endif %}
            {% endfor %}
          </ul>
        </li>
        <li><a href="{{ path('fos_user_security_logout') }}">Se déconnecter</a></li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
